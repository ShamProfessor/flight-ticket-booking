class FlightPrice {
  constructor(flightNumber, customerType, dateType, price) {
    this.flightNumber = flightNumber
    this.customerType = customerType
    this.dateType = dateType
    this.price = price
  }
}

module.exports = FlightPrice
