const FlightInformationService = require('./flight-information.service')
const FlightPriceService = require('./flight-price.service')

module.exports = {
  FlightInformationService,
  FlightPriceService
}
